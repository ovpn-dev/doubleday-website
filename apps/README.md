# Applications

This directory defines the application boundary for Doubleday OS.

The existing Vite marketing application remains at the repository root temporarily so its current deployment continues to work. It will migrate to `apps/website` during the Next.js migration.
