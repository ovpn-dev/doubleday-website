# Consultant Portal

Planned secure workspace for Doubleday consultants: CRM, proposals, project delivery, audits, findings, CAPAs, and client collaboration.

The CRM read model is implemented in `@doubleday/database/crm`. This portal must not expose that data until authentication and role checks are connected; a consultant-facing UI is the next step after selecting the authentication provider.
