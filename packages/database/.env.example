# Copy this file to packages/database/.env and replace the placeholders.
# Prisma reads this file when commands are run with --workspace=@doubleday/database.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/doubleday_os?schema=public"
